const person = {
    name: 'Abhishek',
    age: 31
};

for (let key in person)
    console.log(key, person[key]);

const colors = ['red', 'green', 'yellow'];

for (let color in colors);
console.log(color);